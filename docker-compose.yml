version: '3'
services:
  postgres:
    image: postgres:latest
    container_name: social_media_postgres
    ports:
      - '6500:5432'
    volumes:
      - postgresDB:/data/postgres
    env_file:
      - ./.env.docker
volumes:
  postgresDB:
